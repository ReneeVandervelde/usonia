package usonia.foundation

val FakeSite = Site(
    id = Identifier("fake-site"),
    name = "Fake Site",
    users = emptySet(),
    rooms = emptySet(),
    bridges = emptySet(),
    parameters = emptyMap(),
)
